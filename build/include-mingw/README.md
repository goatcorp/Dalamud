The files in this directory act as no-op hot glue to fix case mismatches
and other cases of things being defined in `foo.h` in MSVC but `bar.h` in MinGW.

FIXME JADE: Fix the include names. Seems like Dalamud's responsible for all of those...
